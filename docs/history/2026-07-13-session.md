# Last session — 2026-07-13

## What happened
- Compared git-hosting options for the recreation project (few hundred MB of images/recordings/renderings; small trusted team, one collaborator already far along). Full analysis in `docs/git-hosting-decision.md`.
- **Decided: Option A — self-hosted Forgejo on the homeserver.** GitHub explicitly rejected, including as a private backup mirror; offsite backup is Google Drive (~half-day recovery accepted).
- **Deployed** (homeserver 192.168.1.100, all verified by the homeserver agent):
  - Forgejo 15 in Docker at `/srv/forgejo/`, 127.0.0.1:3000, LFS enabled, registration disabled, SSH disabled (HTTPS-only git). Admin user `hjalti`; password in `/srv/forgejo/ADMIN_CREDENTIALS` (0600, server-side only).
  - Caddy block `git.spliffdonk.com` → :3000 (pre-edit Caddyfile backup: `/etc/caddy/Caddyfile.backup_forgejo_20260713_082025`).
  - Nightly backup: `/home/hjalti/bin/forgejo-backup.sh` + `forgejo-backup.timer` (03:30 UTC, smoke-tested OK): sqlite integrity-checked copy + full `forgejo dump`, keep 7 local in `/home/hjalti/db_backups/forgejo/`, auto-uploads to `gdrive:backups/forgejo/` once an rclone remote named `gdrive` exists.
- Cloudflare credential in the framework vault turned out to be a **dashboard login, not an API token** → DNS record could not be automated (browser extension also not connected).

## Blockers — user actions pending
1. **Cloudflare DNS**: A record `git.spliffdonk.com` → `50.106.10.250`, **DNS-only (gray cloud)**. Cert auto-issues via Caddy once it exists. (Consider also creating a scoped API token — Edit zone DNS, spliffdonk.com — and storing it in the framework vault so DNS becomes scriptable.)
2. **rclone Google Drive OAuth** on the server: `rclone config` → new remote `gdrive`, type `drive`, scope `drive.file`, auto-config "no", then run the printed `rclone authorize "drive"` on the laptop. Until done, the homeserver holds the ONLY copy.
3. Retrieve admin password (`sudo cat /srv/forgejo/ADMIN_CREDENTIALS`), log in, consider rotating + 2FA. Also consider rotating the Cloudflare dashboard password (it briefly appeared in terminal output this session).

## Next session should start with
1. Verify DNS + HTTPS: `dig +short git.spliffdonk.com` (expect 50.106.10.250) and `curl -sI https://git.spliffdonk.com` (expect 200 + valid cert).
2. Once live: create the `timaflakkarinn` repo on the forge, set up `.gitattributes` LFS tracking (png/jpg/wav/mp3/mp4/psd etc.), create collaborator accounts.
3. Plan the import of the collaborator's existing work (they are far along — their history should come in as the repo's foundation, not be flattened).
4. Push this local folder (docs + session state) as the initial repo, or graft onto the collaborator's history — decide with Hjalti.
5. If desired: add the "shared credentials live in the ai-dev-framework vault" line to the framework's laptop CLAUDE.md template (cross-repo memory request from Hjalti, not yet done).

## Notes
- Homeserver agent (`homeserver-ops`) holds full deploy context + exact rollback inventory in its transcript.
- Project memories written: `forgejo-git-hosting`, `ai-dev-framework-platform`.
